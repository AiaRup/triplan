import React, { Component, Fragment } from 'react';
import { observer, inject } from 'mobx-react';

const root = document.getElementById('root');


class EmailTrip extends Component {
  render() { 
    return ( 
      <React.Fragment>

      </React.Fragment>

     );
  }
}
 
export default EmailTrip;